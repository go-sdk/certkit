# 仓库协作规范

## 文档优先

每次修改代码前，必须先阅读以下文档：

1. `AGENTS.md`
2. `PROJECT_MAP.md`
3. `README.md`
4. 涉及第三方证书容器实现时读取 `DEPENDENCIES.md` 和对应 `internal/*/UPSTREAM.md`

如果文档描述与实际代码不一致，应在同一次修改中更新相关文档，确保文档反映最终实现。

## 修改原则

- 这是个人 Go 证书基础类库，优先保持公共 API 简洁、格式转换明确和向后兼容。
- 公共能力集中在根包 `certkit`；内部格式兼容代码放在 `internal`，子目录不使用 `x` 后缀。
- 修改前检查目录结构、调用关系、第三方边界和 Git 工作区状态，不覆盖或混入无关改动。
- 遵循现有代码风格。Go 结构体字面量超过 3 个字段时展开书写，每行一个字段；同一集合或同一段并列结构体字面量保持统一格式，只要其中一项需要展开，其余项也全部展开。Go 函数声明必须写在同一行，文档中的示例签名可以换行。
- 公共配置使用 `OpenOptions`、`EncodeOptions` 等结构体，不增加 `WithXXX` 风格的公共 Option。
- 代码注释、维护文档和面向使用者的说明统一使用简体中文，只描述最终设计意图、行为和关键约束。
- 错误创建、包装和判断使用 `core/errx`；可供调用方分支处理的错误必须使用固定哨兵错误。

## 安全与兼容约束

- 不输出、记录、提交或保留真实私钥、密码、令牌以及完整证书测试材料。测试私钥必须在测试进程内临时生成。
- 输入解析必须先执行格式和边界校验；不得把结构损坏统一伪装成密码错误。
- 密码探测失败时，只返回已经安全解析的证书元数据，并通过 `ErrPassword` 表示私钥或完整性不可用。
- PKCS12 和 JKS 输出必须显式提供密码，不设置默认输出密码。
- JKS 输入兼容常用 `changeit` store password、`mykey` alias、大小写不敏感 alias、`X509`/`X.509` 证书类型和 PKCS#8 私钥条目。
- JCEKS 和 BKS 仅识别格式，解析和输出固定返回 `ErrUnsupportedFormat`。
- 标准 PKCS12 使用 SSLMate；国密 PKCS12 的本地 fork 仅用于 SM2、SM3、SM4，并保留 `UPSTREAM.md` 记录的安全补丁。
- Mozilla 根证书通过 `x509roots/fallback` 内嵌，不在仓库中复制独立 PEM 文件，也不修改进程级 fallback roots。
- TLS 证书收集可以跳过握手阶段的标准库验证，但最终报告必须独立完成主机名、信任链和服务端链完整性判断。

## 测试边界

- 默认单元测试不得访问真实外部接口。证书链、CRL、OCSP、格式转换和 TLS 握手使用进程内生成证书及本地测试服务。
- 测试不得把私钥、密码或完整 PEM 内容输出到日志和失败信息中。
- 固定时间相关断言，避免依赖测试运行当天的时间。
- DigiCert、Let's Encrypt 和 Amazon Trust Services 的公开 demo 域名，以及 CA 官方仓库、NuGet、Sigstore、badssl 和上游格式样本，只用于显式启用的互操作测试。普通 `make test` 必须跳过这些用例。
- 公网格式样本不得写入仓库或输出完整内容。公开测试私钥只允许在测试进程内解析，用后释放，不得输出或另行保存。
- 互操作测试通过 `CERTKIT_NETWORK_TESTS=1 make test-network` 运行；它验证的是运行时公网状态、真实证书解析和外部格式兼容性，失败时需要区分代码回归、网络限制、上游证书轮换和样本迁移。
- 修改后依次运行 `make lint` 和 `make test`，并检查 `git diff`、`git diff --check` 和工作区状态。涉及在线兼容性且环境允许时再运行 `make test-network`。
- 不执行部署、发布、上传或 `git push`；本地提交需要用户明确要求。

## 提交规范

- 提交信息遵循 Conventional Commits，标题使用简洁英文。
- 需要补充背景、影响或风险时写入提交正文，正文每行不超过 80 个字符。
- 只提交当前任务明确包含并已复核的文件。
