# 上游说明

本目录基于 `go.mozilla.org/pkcs7` v0.10.0 的 `ber.go`，并保留其 MIT
许可证。

本地版本导出 `ToDER` 供 PKCS7 解析入口调用，并补充以下输入边界校验：

- 拒绝空输入和尾随数据；
- 校验长度字段及父子对象边界；
- 校验不定长编码的终止标记；
- 将 ASN.1 嵌套深度限制为 128 层；
- 传播编码过程中的写入错误。

升级上游时，应重新比较 `ber.go`，保留这些边界校验及对应测试。
